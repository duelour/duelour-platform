machine:
  timezone:
    Australia/Melbourne
  node:
    version: 7.8.0
dependencies:
  pre:
    - npm install firebase-tools -g
deployment:
  production:
    branch: master
    commands:
      - cd firebase/
      - ls
      - firebase deploy --token "$FIREBASE_DEPLOY_TOKEN" --non-interactive
